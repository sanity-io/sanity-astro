---
import { useSanityImageUrlBuilder } from '@sanity/astro'
const builder = useSanityImageUrlBuilder()
const {node} = Astro.props
const { width = 960 } = Astro.props.node

const image = builder
  .image(node)
  .width(width)
  .auto('format')
---

<img src={image.url()} />